# Agent instructions (Codex / any non-Claude agent)

The project's standing instructions live in `~/dev/rdp/CLAUDE.md` — read it first and treat it as this
file's content (system facts, commands, safety rules, gotchas, roadmap). Then read
`~/dev/rdp/HANDOFF-CODEX.md` (state as of 2026-09-20, backlog, OPT-046 notes, how to run things, and
the rules for keeping the shared files Claude-compatible). The `OPT-###` tracker is
`~/dev/krdp/research.md`. Append what you did to the "Log" section of `HANDOFF-CODEX.md` (newest first)
so the next Claude session resumes in sync. Do not edit `~/.claude/`.
