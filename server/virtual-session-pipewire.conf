# Dedicated session graph. Launch with a private XDG_RUNTIME_DIR and explicit
# PIPEWIRE_CONFIG_DIR/PIPEWIRE_CONFIG_NAME; never install as the user's default.
# No ALSA/Bluetooth/JACK discovery, physical device nodes, or config includes.
context.properties = {
    core.daemon = true
    core.name = pipewire-0
    default.clock.rate = 48000
}
context.spa-libs = {
    audio.convert.* = audioconvert/libspa-audioconvert
    support.* = support/libspa-support
}
context.modules = [
    { name = libpipewire-module-protocol-native }
    { name = libpipewire-module-metadata }
    { name = libpipewire-module-spa-node-factory }
    { name = libpipewire-module-client-node }
    { name = libpipewire-module-access }
    { name = libpipewire-module-adapter }
    { name = libpipewire-module-link-factory }
]
context.objects = [
    { factory = metadata args = { metadata.name = default } }
    { factory = spa-node-factory args = {
        factory.name = support.node.driver
        node.name = KRDP-Virtual-Driver
        node.group = pipewire.dummy
        priority.driver = 20000
    } }
    { factory = adapter args = {
        factory.name = support.null-audio-sink
        node.name = krdp.virtual-session.audio
        node.description = "KRDP Virtual Session Audio"
        media.class = Audio/Sink
        audio.position = [ FL FR ]
        node.virtual = true
        adapter.auto-port-config = { mode = dsp monitor = true position = preserve }
    } }
]
